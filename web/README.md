# Web UI

Vite + React frontend for **pdf-inspector-service**.

```bash
# from repo root
bun run web:dev    # dev server :5173 (proxies /v1 to API)
bun run build:web  # production assets → web/dist (served by API)
```

See the root [README](../README.md).
